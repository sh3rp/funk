syntax = "proto3";

package funk;

message Event {
    string id = 1;
    int64 timestamp = 2;
    string src = 3;
    repeated string tags = 4;
    bytes data = 5;
}

message ReturnValue {
    repeated string tags = 1;
    bytes data = 2;
}